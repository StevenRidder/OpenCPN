# OpenCPN Baselines

Place captured OpenCPN Chart 1 baseline PNGs here using the paths named in
`../baseline.comparison.json`.

This directory intentionally starts without generated images. The comparison
manifest records the required slots and tolerances; QA-2 should provide the
repeatable capture and image-diff command that fills and checks these files.
